<div class="col-span-1 h-58">
  <%= render Maglev::Uikit::ImageLibrary::CardComponent.new(
    filename: asset.filename,
    image_url: public_asset_url(asset, host: maglev_config.asset_host, only_path: maglev_config.asset_host.blank?),
    width: asset.width,
    height: asset.height,
    byte_size: asset.byte_size,
    delete_url: editor_asset_path(asset.id, query: params[:query], page: params[:page])
  ) %>
</div>