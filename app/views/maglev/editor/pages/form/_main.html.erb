<div class="mt-2 grid grid-cols-1 gap-4">
  <%= f.text_field :title %>

  <%= f.text_field :path unless f.object.index? %>

  <%= f.check_box :visible, placeholder: t('maglev.editor.placeholders.pages.visible_placeholder') %>
</div>
